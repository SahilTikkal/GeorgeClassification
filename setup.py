from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Classifying if the image contains St. George or not.',
    author='Sahil_Tikkal',
    license='',
)
